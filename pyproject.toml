[project]
name = "preference-experiment"
version = "0.1.0"
description = "Experiment to detect self-preference bias in LLMs"
requires-python = ">=3.9"
dependencies = [
    "httpx",
    "python-dotenv",
]
